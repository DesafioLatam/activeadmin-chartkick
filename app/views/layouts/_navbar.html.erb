<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="#">Proyecto Base</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">
      <% if user_signed_in? %>
        <li class="nav-item">
          <a class="nav-link" href="<%= destroy_user_session_path %>" data-method="delete"> Salir </a>
        </li>
      <% else %>
        <li class="nav-item active">
          <a class="nav-link" href="<%= new_user_session_path %>"> Ingresar <span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= new_user_registration_path %>"> Registrarse </a>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
